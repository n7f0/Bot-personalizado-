version: '3.8'

services:
  discord-rpc:
    build: .
    container_name: discord-rpc-bot
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - rpc-data:/app/data
    restart: unless-stopped

volumes:
  rpc-data:
  
